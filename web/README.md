# web

The public Next.js app: `/vote`, `/live`, `/incidents`, and the API routes that back them.

See the root [`README.md`](../README.md) for how to run this alongside the rest of the workspace, and
[`CLAUDE.md`](../CLAUDE.md) for the architecture and content model. `AGENTS.md` in this folder is Next.js's
own agent note - read it before writing Next code here.
